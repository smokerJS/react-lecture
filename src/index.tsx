import React from "react";
import ReactDOM from "react-dom/client";
import App from "App";

const root = ReactDOM.createRoot(
  document.getElementById("root") as HTMLElement
);
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

/**
 *
 * 자바스크립트 설정이나 초기 셋팅값을 작성
 *
 */
