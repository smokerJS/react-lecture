import React, { FC } from "react";

/**
 *
 * 컴포넌트와 JSX
 * 컴포넌트는 리액트를 구성하는 가장 작은 단위
 * JSX = JS 파일에서 HTML 문법을 사용 가능하게 해주는 확장자
 * TSX = TS 의 JSX
 * 타입스크립트로 컴포넌트를 작성할때에는 컴포넌트라고 타입을 명시해주어야함
 *
 */

const Sduty1Page: FC = () => {
  return (
    <div>
      <div>test</div>
    </div>
  );
};

export default Sduty1Page;
